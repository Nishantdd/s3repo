import { Metadata } from 'next';
import Page from './page';

export const metadata: Metadata = {
    title: 'S3Repo - Login',
    description: 'Login to your S3Repo dashboard'
};

export default Page;
