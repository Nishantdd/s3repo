import { Metadata } from 'next';
import Page from './page';

export const metadata: Metadata = {
    title: 'S3Repo - Dashboard',
    description: 'Manage your stored images in the S3Repo dashboard.'
};

export default Page;
